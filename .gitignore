# Tender / govt reference material (sensitive)
reference/

# Tool caches / local state
.gstack/
.playwright-mcp/

# Screenshots (mockup iterations) — only at repo root, not inside app
/*.png

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Node
node_modules/
dist/
.env
.env.local
*.log

# Allow Laravel vite build output (needed for shared hosting deploy)
!portal-jpph/public/build/
